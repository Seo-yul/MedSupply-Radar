.venv/
__pycache__/
*.py[cod]
.DS_Store
.streamlit/secrets.toml
.agents/
.claude/
docs/superpowers/
.env

# 블라인드 평가 스냅샷(Task S-22) — DB·봉인 라벨은 생성 산출물이라 미커밋. manifest.json은
# 봉인 증거(해시 매니페스트)라 예외적으로 커밋 대상이다(data/blind/manifest.json).
data/blind/*
!data/blind/manifest.json

# judge 실행 결과(Task S-27, eval/judge.py::run_experiment 산출) — 실행마다 달라지는 jsonl이라
# 미커밋. S-28 본실행 산출물은 그 시점에 커밋 여부를 별도 판단한다(task-S27-brief.md §격리).
eval/results/*
!eval/results/.gitkeep

# 로컬 산출 데이터(재생성 절차: README 빠른 시작 — 앵커 c34bf4cb 검증)
data/medsupply.db
data/medsupply.db-wal
data/medsupply.db-shm
data/llm_cache.db
